[MENTAL ANCHOR]
Project: Digital Custody Asset (DCA) as a Service Platform
Phase: PostgreSQL Centralized Database API, Soft Archiving Policy & OWASP REST Security
Date: 2026-09-02
Author: Jules

Summary:
- Centralized Database API Access Layer implemented in `src/dca_service/adapters/database_api.py` (`DatabaseAPI`), managing all direct PostgreSQL table access (users, assets, cloverleaf scores, revenue splits, role permissions).
- Non-deletion soft user archiving policy enforced: `DELETE /api/users/{username}` invokes `DatabaseAPI.disable_and_archive_user(username)` setting `is_active=False`, `is_disabled=True`, `can_login=False`, `is_archived=True`, and `tags=['archive']` without deleting any database records.
- OWASP REST Security Cheat Sheet guidelines enforced across web layer (`src/dca_service/web_app.py`): generic auth failure messages (`"Authentication failed. Invalid username or password."`), CSRF and Origin validation on user mutation endpoints (`create_system_user`, `reset_user_password`, `delete_system_user`, `register_user`), strict `sslmode=verify-full` check on TCP `DATABASE_URL` connections, and server-side exception logging.
- `docs/schema.sql` updated with idempotent `ALTER TABLE` statements and unique constraint migrations for `users`, `role_permissions`, `assets`, `cloverleaf_scores`, `revenue_splits`, `sub_accounts`, and `blockchain_transactions`.
- Unit, scenario, performance, and Playwright E2E integration test suites updated with 100% pass rate (1,261 passed).
- DSOM Pre-commit guardrails (Ruff, Mypy --strict, SUMMARY.md auto-generator, Pytest) executed cleanly.
